<table>
  <thead>
    <tr>
      <th width="75">Created</th>
      <th width="125">Due from</th>
      <th width="200">Name of IOU</th>
      <th width="100">Amount</th>
      <th width="50">Paid?</th>
      <th width="25">Go</th>
    </tr>
  </thead>
  <tbody>  
    <% @receivable_ious.each do |iou| %>
    <tr>
        <td><%= iou.created_at.strftime("%m/%d/%y") %></td>
        <td><%= iou.contact.first_name + " " +   iou.contact.last_name %> </td>
        <td><%= iou.name %> </td>
        <td><%= number_to_currency(iou.balance) %> </td>
        <td><%= iou.status %> </td>
        <td><%= link_to "View", "/ious/#{iou.id}", method: :get %> </td>
      </tr>
    <% end %>
  </tbody>
</table>